# read02 学习记录

## 学习方式

- 按 AGENTS.md 第二节“精读引导流程”执行。
- 逐句推进，一句一问，由用户独立读，做三档标注（✅ 认识 / ⚠️ 不确定 / ❌ 不认识），先不查词、不说意思。
- 段末才汇总段意并抽认。
- 用户先说，再补漏；不主动整句翻译。
- 只练习精读，不练考场定位模式。

## 进度

- 已完成：准备 `read02.md`（Reading Part A, Text 2，第 31–35 题）。
- 已完成：确定阅读顺序为题干 → 正文 → 选项。
- 当前步骤：正文第 1 段，等段意和主干。

## 阅读顺序

- 题干 → 正文 → 选项。
- 考场上读题干是为了定位找答案；精读时读题干是为了暴露题干生词、建立主题预期、读完对账。
- 选项放在最后，避免干扰项的措辞先入为主。
- 题干先行，但不许变成“扫读找答案”，正文仍要逐段过完。

## 第 1 步：题干（第 31–35 题）

已通过：

- 31：能理解科学家在沙漠／荒地／山区上空飞行，为的是寻找地下的东西。
- 33：识别为主旨题，理解 `best title suggested below` 为“从下面给出的标题中选最好的”。
- 34：正确理解 `NOT mentioned`。
- 35：识别句子不完整，缺“用什么”的成分。
- 31–35 题干整体大意均能说清，无阻断性误解。

暴露：

- `hilly`：误判为名词，当成与 `desert` 并列的事物。未识别 `-y` 形容词后缀（hill → hilly）。
- `hill`：不认识。已补：小山、山丘，与 mountain 大小不同。
- `wasteland`：不认识，按 waste「浪费」+ land 猜成「垃圾站」。已补：waste 作形容词表“荒芜的”，wasteland = 荒地。
- `study`：先理解为“学习”，提示后能判断为“研究”。
- `suggested`：只理解为“建议”，未掌握过去分词作定语表示“给出的、提出的”。
- `ground`：不清楚 `in the ground` 的译法。已解决：`in the ground` 地下 / `on the ground` 地面。用户表示本次已学会，不收录。
- `minerals` 拼写为 `minierals`。

第 32 题并列成分：

- 问“被研究的对象有几个”，答“两个（branches、roots）”，漏掉 `trees`。
- 未把 `A, B and C` 识别为三项并列，把 `of trees` 和 `branches and roots` 分成了两层。
- 已补规则：并列三项写作 `A, B and C`，`and` 前的逗号表示列举未完；数并列项要数到 `and` 为止。

## 第 2 步：正文第 1 段

分段自述记录：

- 第 1 句：标出 `bushes`；`trained eyes` 直译为“被训练过的眼睛”。
- 第 2 句：标出 `metal`、`probable`；把 `After an hour's flight` 理解成“在几个小时的飞行之后”。
- 第 3 句：把 `Gold possible` 译成“金色可能”，`gold` 取了颜色义。
- 第 4 句：大意正确。
- 第 5 句：`Uranium` 未表态，只照抄原词。

待完成：

- `an hour's flight` 的时长表达待纠正。
- `gold` 在本文的义项待确认。

补充记录：

- `bushes`：先猜“草”，错误。已补：灌木丛，与 grass 不同。
- `probable`：猜不出。已补：很可能的；并补 `probable` 与 `possible` 的可能性程度差别。
- 段意：初次提交“好些科学家，通过这种方式，在这片土地上寻找东西”，过于笼统，没有点明方式和目标，需要重做。
- 主干：不清楚“主干”指什么，需要先讲方法。

段意重做（第一次）：

- “几组”：答 4 组，正确。
- “找什么”：答 metal，正确。
- “什么方式”：只答出“有坐飞机的，有步行的”，未说出他们在做的动作（观察地表），还需要补。
- 主干：仍未提交。

主干（第一次提交）：

- 答案：`two scientists look down bushes and trees`。
- 正确：主语 `two scientists`，谓语 `looked down`；`Flying over...` 和 `with trained eyes` 两个状语都剥掉了。
- 错误一：宾语前缺介词 `at`，写作 `look down bushes and trees`。
- 错误二：时态写成 `look`，原文是 `looked`。
- 已补：`look` 是不及物动词，接“看什么”必须带介词；`look at` 看 / `look down at` 俯视 / `look up at` 仰视。

待完成：段意终稿、本篇第一阶段抽认。

段意终稿（第 1 段）：

- 提交“科学家们用飞机和步行两种方式，在地面上寻找 metal”，通过。
- 小修正：“在地面上”不准确，飞机上那两组不在“地面上”，应说“在这片区域”。

抽认（第 1 轮，题干 + 正文第 1 段）：

- `hilly` → 答“丘陵”，词性错，应是形容词“多丘陵的、起伏的”；名词“丘陵”是 `hill`。
- `wasteland` → 答“荒芜山地”，多了“山”，应是荒地、荒原。
- `bushes` 灌木 ✓、`probable` 很可能的 ✓、`suggested` 被提出的 ✓、`gold`（本文）金子 ✓、`an hour's flight` 一小时的飞行 ✓、`look down at` 往下看 ✓。

## 第 3 步：正文第 2 段

- 用户主动求助第 1 句：只标出 `surface` 不认识，但整句读不通。
- 明确要求只给方法、不代做，因此本节先给“读不通句子的处理流程”，不直接讲解该句。
- 已教流程：圈否定词 → 看冒号判断是否重说 → 找两半句主干 → 生词先当空格 → 用搭配和上文给空格定范围 → 代词还原后再读 → 用中文说出验证。

第 1 句独立分析结果：

- 否定词：只数出 `no`，并反问 `None` 算不算。实际是两个：`None`、`no`。
- 主干：判断“两个 `had` 是各自的动词”，正确。
- `surface`：靠“the earth's □ + below”推出“表面”，第 5 步方法用对了。
- 问题一：翻译时丢掉 `magic`，把“他们没有魔力”译成“他们没有向下看的能力”，意思偏了。
- 问题二：没有回答冒号后面是“重说”还是“新信息”。

第 1 句结账：

- 冒号：答“重说前面，是对 X-ray eyes 的另一种表述”，正确。
- 本句补漏候选：`None of the scientists`（全否定）、`surface`（表面）、`magic power`（魔力，不能丢）。

第 2 句（第一次）：

- 自述“单词基本上都会，但不太懂”，是典型的“假懂”：每个词都认识，连起来不通。
- 译文：“他们是仅仅放使用本地金属西方方法中的一个，在地面上”。
- 错误一：`put ... to use` 按字面读成“放使用”，未识别“把……投入使用”这个搭配。
- 错误二：`newest` 读成“西方”，未识别 `new → newer → newest` 最高级。
- 错误三：`locating` 读成“本地”，与 `local` 混淆。
- 错误四：`minerals` 译成“金属”，与 `metal` 混淆。
- 错误五：`in the ground` 又写成“在地面上”，此前已纠正过一次。

第 2 句（重译）：

- 译文：“他们使用最新的在地面下，定位矿藏的方法”，结构正确，通过。
- 遗漏一：`merely` 丢了。上一版译出过“仅仅”，这一版丢失；它是与第 1 句对比的关节。
- 遗漏二：`one of the newest methods` 漏了“之一”。`one of the + 最高级 + 复数名词` 结构待巩固。

第 2 句主干：

- 主语：答 `they`，正确。
- 谓语：只答 `were`，漏了实义动词。谓语是 `were putting`（过去进行时），`were` 只是助动词。
- 已补规则：找谓语要把助动词和实义动词一起抓，`were` 单独一个没有意思。

第 2 句后半截（碎片）：

- 译出 `signs` 标记、`may lie beneath`、`growing`，大意能串起来。
- `certain`：译成“确定的”，错误，此处是“某些”。
- `lie`：译成“躺在”，此处应是“位于、坐落”。
- `which`：答“标记”，错误，应指 `the ground`；未用“代回试读”验证。
- 断点：未察觉这半截缺谓语。

断点追问：

- 问“缺的是哪一类词”，答“不知道”。
- 已补：缺的是主句的谓语动词。
- 已教“括弧法”：先把 `that` / `which` / `who` 引出的从句括起来，剩下的才是主句；主句必须有谓语，找不到谓语就不是句子。

括弧法迁移测试（第 2 段结束后）：

- 句 `The book that I bought yesterday is on the table.`：答主语 `The book`、谓语 `is`，正确，方法已迁移。

第 2 段抽认：

- 通过：`merely` 仅仅、`put A to use` 把 A 投入使用、`locating` 定位、`certain` 某些、`beneath` 在……下面、`surface` 表面、`magic power` 魔力、`None of the scientists` 一个也没有。
- 未通过：`lie`（本句义项）答“不知道”。已补：位于、坐落，搭配 `lie beneath / lie in`。
- 指代：`they` 答“科学家们”，正确。

第 2 段段意（第一次）：

- 提交：“他们使用最新的找矿藏的方法之一，找那些树和植物，下面可能有矿藏。”
- 缺第 1 句内容（没有透视眼、没有魔力），丢掉“不是靠魔法，是靠观察”这层对比。
- 把 `trees and plants as signs` 读成“寻找树和植物”，方向反了。

第 2 段段意（第二次）：

- 提交：“科学家们没有透视眼，他们使用最新的找矿藏的方法之一，在那些树和植物下面，可能有矿藏。”
- 进步：第 1 句已纳入。
- 仍未回答“树和植物是被找的东西，还是用来判断的东西”。
- 段意里方法的核心（把树和植物当作标志）仍未点出。

第 2 段段意（第三次）：

- 提交：“科学家们没有透视眼，他们只是用一种最新的找矿方法：把树和植物当作标记，来判断下面可能有矿藏。”
- 通过。第 2 段结账。

## 第 4 步：正文第 3 段

第 1 句（全段仅此一句）：

- 大意基本对：方法基于一个事实，地下的矿物会影响地表的灌木和树木。
- `the kind of`：读成“部分”，错误，应为“种类”。
- `the fact that ...`：未说明 `that` 从句是在交代 fact 的内容。
- 主干：未提交。

- 主干：答主语 `the parts of the forest trees`，正确；谓语只答 `were burned`，漏了并列的 `tested`。

第 6 句：

- 主语 `Each small part`、谓语 `was examined`、句意都对，`each` + 单数谓语已用上。
- 提问 `learn` 能否译“学习”。已补：此处是“得知、弄清楚、了解到”；`learn` 接语言或技能表“学习”，接 `that` / `whether` / 疑问词从句表“得知”。
- 指代 `it` 未提交。

- 指代 `it`：答“指上面 burned to ashes”，错误，应指 `each small part`。
- 已补两条验证法：① 单复数一致，`it` 是单数，`ashes` 是复数，`each small part` 才有资格；② 代回试读，`in it` 后面需要东西或地方，动作短语不行。

- 用户反问 `each small part` 指什么。已答：回指上句的 `the parts of the forest trees`，也就是前面收集的 tree seeds、small branches、roots。
- 已补规则：回指不只有代词，名词短语也会回指。

第 4 段抽认：

- 8 项全过：`brook` 小溪、`seeds` 种子、`gathered` 收集、`be filled with` 被……填满、`dug` 挖开（`dig - dug - dug` 也答出）、`ashes` 灰、`learn` 了解/弄清、`Each bag and box** was** marked` 用单数。
- 待完成：第 4 段段意。

第 4 段段意：

- 提交：“一个找矿小组在 Watson Bar Greek，采集了树的根，树枝，种子，送到实验室烧成灰、检测，为了弄清到底有没有矿藏在里面。”
- 通过。第 4 段结账。

## 第 6 步：正文第 5 段

第 1 句：

- 并列项数：答“三个”，正确（roots、branches、seeds）。
- 主干：主语 `Study of the roots, branches, and seeds`，谓语 `showed`，正确。
- 句意基本正确；`showed` 译成“展示”，应为“表明、显示”。

第 2 句：

- 句意正确：“但是有一些金子在树根里，更小量的金子在树枝和种子里面。”
- 用户反问 `there` 的作用。已补：这是 `there be` 存在句 = 有；`there` 是引导词，本身无义，真正的主语是 `be` 后面的名词。
- 已补：`there be` 的单复数看 `be` 后面第一个名词；`small amounts of gold` 是复数，所以用 `were`。

第 3 句：

- 句意正确：最靠近树干的种子，比长在树枝末端的种子含金更多。
- `trunk` 未标生词，从上下文推出“树干”，推词能力正常发挥。
- 分词短语作后置定语（`growing nearest to the tree trunks`）理解无误。
- 主干已代补：`The seeds had more gold than those`。
- 指代 `those`：未提交。

- 指代 `those`：答“树枝和树根”，错误，应为 `the seeds`。
- 已补新方法：判断 `those` / `it` 的指代，除了代回试读，还要看它**后面跟的修饰语**——修饰语会限定它只能是什么；`growing on the ends of the branches` 只有“种子”说得通。

第 5 段抽认：

- 6 项全过：`showed no silver` 表明没有银、`small amounts of` 少量的、`a little less` 更少一点、`there be` 单复数看 `be` 后面第一个名词、`trunk` 树干、`those` 指 seeds。
- 待完成：第 5 段段意。

第 5 段段意：

- 用户按骨架填：银子、金子、靠近树干的、树枝末端，内容正确。
- 完整表述：化验过树根、树枝、种子之后，没找到银，只有少量的金，而且越靠近树干的种子含金越多。
- 通过。第 5 段结账，正文全部读完。

## 第 7 步：选项

短选项（Q31、Q34、Q35）：

- Q34 全对：Leaves 树叶、Roots 树根、Branches 树枝、Seeds 种子。
- Q31：`gold` 金 ✓、`silver` 银 ✓、`uranium` 答“矿”，错误，应是“铀”；`minerals` 答“金属”，错误，应是“矿物”。
- `minerals` 反复错：第一次译“金属”，抽认时答对“矿物”，这次又答回“金属”，已列入拼写或词义提醒。
- Q35：`X-ray` X 光 ✓、`magic power` 魔力 ✓、`trained eyes` 训练过的眼睛（字面对，应为“训练有素的眼睛、专业眼光”）。
- `a special instrument`：不认识。已补：仪器、工具。
- 已补辨析组：`mineral` 矿物 / `metal` 金属 / `mine` 矿（矿坑、矿山）/ `ore` 矿石 / `uranium` 铀。

Q32 选项：

- [A] 正确：树枝处比种子处多。
- [B] **反向错误**：原句 `smaller amounts of gold in the roots than in the branches` 是“树根处比树枝处少”，译成“树根处比树枝处多”。
- [C] 事实正确（表达方向与原文相反），但把 `the tree trunks` 写成“树根”，`trunk` 与 `root` 混淆。
- [D] 正确。
- 已补规则：比较方向由比较词决定，不由 `than` 那半边决定；`more / larger A than B` → A 比 B 多；`less / fewer / smaller A than B` → A 比 B 少。用户把 `smaller` 当作“小”而非“更少”。
- 已注明：试卷 `there were less amounts of gold` 搭配不规范（`less` 接不可数），不作为正确用法记忆。

Q33 选项：

- 四个选项全部理解正确：[A] 用特殊能力搜寻金属、[B] 新的找矿方法、[C] 通过树和植物可以找到金子、[D] 一个用树和植物找矿的新方法。
- 进步点：`Metals` 译“金属”、`Minerals` 译“矿藏”，同一题内已能分清，不再混淆。
- 选项阶段结束。

## 第 8 步：全篇抽认

抽认完成（2 轮共 18 项），未通过项已全部补回。

## 生成的笔记

- 已生成 `read02-review-notes.md`。
- 行数 70，大小 3.3 KB，符合 45～70 行、2～4 KB 要求。
- 已按“用户已掌握”删除：`in the ground / on the ground`（用户明确说本次已学会）、`brook`、`a little less`、`the fact that` 同位语从句（用户当次答对）。

## 第 9 步：做题检验

第一次作答：`31D　32C　33B　34A　35B`（3 对 2 错）。

- 31 D：正确。
- 32 C：正确。
- 33 B：错误，需要追溯依据。
- 34 A：正确。
- 35 B：错误，需要追溯依据。
- 处理方式：不直接给答案，先让用户回原文找 33、35 的依据。

追溯依据的结果：

- 33：给出的是印象（“主旨题，全篇都在讲找矿藏”），没有具体原文依据；未比较 [B] 与 [D] 的字面差别。
- 35：推理是常识排除（“X 光不能选、特殊工具不能选、训练的眼睛也不对”），全程未回原文找依据；`trained eyes` 其实出现在第 1 句。
- 新暴露：做题时用“感觉对不对”代替“回原文找依据”。

33 追溯结果：

- 用户自己抓住关键：[B] 的 `Methods` 是复数，原文只有一种方法。
- 但把“方法”指错了：以为方法是“烧成灰”；实际烧成灰和检测是实验室验证步骤，方法本体是“用树和植物当标志找矿”。
- 排除理由正确：[A] 说特殊能力，原文明确否认；[C] 只提金子，范围太窄。
- 正确答案 [D]。用户已自行推出。

35 追溯结果：

- 用户在原文第 1 段第 1 句找到 `trained eyes`，正确。
- 正确答案 [D]。
- 错因定位：[B] `magic power` 与 [A] `X-ray` 都出现在原文，但原文对二者是**否定**（`no magic power`、`had no X-ray eyes`）。用户只看到词面重合，没有看原文对它的态度。
- 重复薄弱点：read01 第 28 题选 Aristotle 就是同一类错误（看到原文出现的词就选）。这是第二次。

最终成绩：

- 31D　32C　33D　34A　35D
- 用户作答 `31D　32C　33B　34A　35B`，3 对 2 错。
- 两道错题都不是英语词汇或句法问题，而是同一类做题动作缺失：不回原文核对否定与范围。
- 按 AGENTS.md，这两条做题归因只留在学习记录，不进 `read02-review-notes.md`。

## 跨篇记录

- 已新建项目根目录 `exam-log.md`，横向记录做题问题。
- 本篇幅贡献两条，均与 read01 重复：“看到原文出现过的词就选”“忽略选项里的单复数”“不回原文核对，凭感觉排除”。

第 1 轮（9 个）：

- 通过 8 个：`hilly` 丘陵的、`merely` 仅仅、`put A to use` 把 A 投入使用、`certain` 某个、`lie` 位于、`brook` 小溪、`dug` 挖开、`ashes` 灰。
- 未通过：`wasteland` 答“不认识”。已再补：荒地、荒原；`waste` 此处是形容词“荒芜的”，不是“浪费”。

第 2 轮（9 个）：

- 通过：`suggested` 被提出的、`be based on` 基于、`gathered` 收集、比较方向（答“than 后面多”，方向判断已改对）。
- 未通过：`beneath` 答“不认识”（已补：在……下面）；`learn` 答“表明”（应为得知、弄清楚，表明是 `show`）；`instrument` 答“不认识”（已补：仪器、工具）；并列被动答“不知道”（已再补：共用一个助动词）。
- `trunk` 本轮未答，前一轮曾答对“树干”。

- 主干用户跳过，已代补：`Roots were dug and put`。
- 约定：短句不再追问主干，长句必问。

第 4 句：

- 问“主语是两个东西，为什么谓语用 was”，答“不知道”。
- 已补：`each` 强调“每一个”，谓语一律用单数；`Each bag and box was marked.` 对比 `The bags and boxes were marked.`；`every` 同理。
- 已补句意：每个袋子和每个盒子都被仔细做了标记；`marked` 做标记（被动），`carefully` 仔细地。
- 测试题 `Every bag and box ___ / The bags and boxes ___`：答 `was / were`，正确。

第 5 句：

- `scientific laboratory`：自述“我猜的”，猜对（科学实验室）。
- `the parts of the forest trees`：读成“一部分树木”，错误，应为“森林树木的各个部分”。`A of B` 的中心词在 A 这一规则未掌握。
- 标出生词：`forest` 森林、`burned` 烧（`burn` 的过去分词）、`ashes` 灰。
- 识别出两个动作（被烧、被检测），并列被动能认出来。
- 主干：未提交。

- 主干：答主语 `a mineral search group`、谓语 `gathered`，正确；宾语未答，补 `bags of tree seeds`。

第 2 句：

- 主语 `Boxes`、谓语 `were filled`、句意“盒子被小树枝装满”，全部正确。
- 已补搭配：`be filled with` 被……装满。

第 3 句：

- 句意：答“树根被 dug，然后被放进盒子里”，两个动作都识别出来了。
- `dug`：不认识，未译。已补：挖；`dig` 的过去分词，`dig - dug - dug`。
- 已补句法：并列被动共用助动词，第二个 `were` 省略，不要因为 `put` 前没有 `were` 就当成主动。
- 主干：未提交。
- 指代：`This` 未提交。

第 3 段追问结果：

- `the fact that ...`：答“同位语，说明这个事实是什么”，正确。
- `the kind of`：改为“种类”，正确。
- 主干、`This` 指代仍未提交。

- 主干：答主语 `newest method`、谓语 `is based`，正确。已补：`be based on` 是固定搭配“基于”，整体作谓语。
- `This` 指代仍未提交。

- `This` 指代：答“上一句的方法”，正确，回指第 2 段的找矿方法。第 3 段结账。

## 第 5 步：正文第 4 段

第 1 句：

- `Watson Bar Greek`：读成“华盛顿”，把地名当成了 Washington。
- `six thousand feet high`：译成“六千英尺长”，`high` 应为“高”。
- `gathered`：读成“集合”，此处应为及物动词“采集、收集”。
- `seeds`：不认识；`bags of tree seeds` 未理解。
- 切句错误：把地点状语和同位语当成独立句子，未识别全句只有一个主语 `a mineral search group`。
- 主干：未提交。
